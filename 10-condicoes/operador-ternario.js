        // Operador ternário (três blocos)
        // [teste] ? [true] : [false]
        console.log("Operador ternário")
        var media = 9
        console.log(media >= 7 ? "Aprovado" : "Reprovado")